variables:
  RELEASE: 2.9
pool:
  vmImage: 'windows-latest'
  
steps:
  - task: DownloadGitHubRelease@0
    inputs:
      connection: 'captncraig'
      userRepository: 'StackExchange/dnscontrol'
      defaultVersionType: "v$(RELEASE)"
      itemPattern: 'dnscontrol.exe'
      downloadPath: '.'
  - script: "ls -la"
  - script: "choco pack --version=$(RELEASE)"
